"""
"""

def pan(num):
	for n in range(len(str(num))):
		if(str(n+1) not in str(num)):
			return(False)
	return(True)
def formsPan(n1,n2,prevNums):
	if(pan(str(n1)+str(n2)+str(n1*n1))) and str(n1)+str(n2)+str(n1*n2) not in prevNums):
		return(True)
	return(False)
	
total=0
prevNums=[]
print(formsPan(39,186,39*186,[]))
#for x in range(1,10000):
#	for y in range(1,x+1):
		if(formsPan(x,y,prevNums)):
			tot+=(x*y)
			prevNums.append(str(n1)+str(n2)+str(n1*n2))
