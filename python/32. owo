def fac(num):
	tot=0
	for n in range(len(str(num))):
		i=1
		for x in range(int(str(num)[n])):
			i=i*(x+1)
		tot=tot+i
	return(tot)
	
total=0
for x in range(3,1499999):
	if(x==fac(x)):
		total=total+x
print(total)
