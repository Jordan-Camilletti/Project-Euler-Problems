""
""

import itertools.permutations

def findPerms(n):
	tot=0
	for perm in permutations(n):
		if("""Is a cube"""):
			tot+=1
	return(tot)

cubes=[]
num=1
while(findPerms(n)!=5):
	cubes.adjoin[num*num*num]
	num+=1
