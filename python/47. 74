"""The first two consecutive numbers to have two distinct prime factors are:

14 = 2 × 7
15 = 3 × 5

The first three consecutive numbers to have three distinct prime factors are:

644 = 2² × 7 × 23
645 = 3 × 5 × 43
646 = 2 × 17 × 19.

Find the first four consecutive integers to have four distinct prime factors each. What is the first of these numbers?"""
import math

def isPrime(num):
	for n in range(2,math.ceil(num/2)+1):
		if(num%n==0): return(False)
	return(True)

def primeD(num):
	for n1 in range(2,num):
		for n2 in range(2,n1+1):
			if(isPrime(n1) and isPrime(n2) and n1*n2==num):
				return([num,n1,n2])
	return([0,0,0])

ans=[0,0,0]
num=1
while(True):
	num+=1
	if(primeD(num)[0]!=0):
		if(primeD(num)[0]==ans[0]+1 and ans[1] not in primeD(num) and ans[2] not in primeD(num)):
			print(ans[0],primeD(num)[0])
		else:
			ans=primeD(num)
