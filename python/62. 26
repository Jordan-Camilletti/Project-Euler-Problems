""
""

from itertools import permutations

def findPerms(n):
	tot=0
	for perm in permutations(n):
		if((perm**(1/3))%1==0):
			tot+=1
	return(tot)

cubes=[]
num=1
while(findPerms(num)!=5):
	cubes.adjoin[num*num*num]
	num+=1
print(num)
