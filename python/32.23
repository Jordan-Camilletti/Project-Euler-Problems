"""
"""

tris=[]
for c in range(999):
	for b in range(c):
		for a in range(b+1):
			if(a+b+c<=1000 and (a*a)+(b*b)==(c*c)):
				tris.append((a,b,c))
score=[]
for n in range(1000):
	score.append(0)
for t in tris:
	score[t[0]+t[1]+t[2]-1]+=1
topN=0
top=0
for t in range(len(tris)):
	if(tris[t]>topN):
		topN=tris[t]
		top=t
print(t+1)
