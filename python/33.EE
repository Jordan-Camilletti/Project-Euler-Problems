"""
"""

def haveSim(n, d):
	for w in str(n):
		if(w in str(d) and int(w)!=0):
			return True
	return False

def cancel(n,d):
	newN=""
	newD=""
	for w in str(n):
		if(w in str(d)):
			newN=str(n).replace(w,"")
			newD=str(d).replace(w,"")
	return(int(newN)/int(newD)==n/d)

for y in range(10,101):
	for x in range(10,y):
		if(haveSim(x,y)):
			print(cancel(x,y))
