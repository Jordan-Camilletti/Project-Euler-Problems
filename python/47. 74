import math

def isPrime(num):
	for n in range(2,math.ceil(num/2)+1):
		if(num%n==0): return(False)
	return(True)

def divis(num,used):
	for n1 in range(2,num):
		for n2 in range(2,n1+1):
			if(n1*n2>num):
				break
			for n3 in range(2,n2+1):
				if(n1*n2*n3>num):
					break
				for n4 in range(2,n3+1):
					if(n1*n2*n3*n4>num):
						break
					if(isPrime(n1) and isPrime(n2) and isPrime(n3) and isPrime(n4) and n1*n2*n3*n4==num):
						if(n1 not in used and n2 not in used and n3 not in used and n4 not in used):
							return([n1,n2,n3,n4])
	return([0,0,0,0])

used=[]
num=1
#print(divis(210,used))
"""x=[3,4]
used+=x
print(used)
print(3 in used)"""
while(True):
	num+=1
	if(divis(num,used)[0]!=0):
		print(num)
		print(divis(num,used))
		used+=divis(num,used)
		#print(used)
