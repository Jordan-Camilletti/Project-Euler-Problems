"""The first two consecutive numbers to have two distinct prime factors are:

14 = 2 × 7
15 = 3 × 5

The first three consecutive numbers to have three distinct prime factors are:

644 = 2² × 7 × 23
645 = 3 × 5 × 43
646 = 2 × 17 × 19.

Find the first four consecutive integers to have four distinct prime factors each. What is the first of these numbers?"""
import math

def isPrime(num):
	for n in range(2,math.ceil(num/2)+1):
		if(num%n==0): return(False)
	return(True)


n1=2
n2=2
n3=2
ans=[]
ans2=[0,0]
"""while(not isPrime(n1) or not isPrime(n2) or n1*n2!=ans1+1):
	if(isPrime(n1) and isPrime(n2)):
		ans1=n1*n2"""
for n1 in range(2,25):
	for n2 in range(2,n1+1):
		for n3 in range(2,n2+1):
			if(isPrime(n1) and isPrime(n2) and isPrime(n3)):
				ans.append(n1*n2*n3)
				"""print(n1,n2)
				if(n1*n2==ans1+1 and ):
					print(ans1+1)
				else:
					ans1=n1*n2"""
print(sorted(ans))
